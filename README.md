Backend: Node JS, MongoDB

1. npm initi -y

2. npm install -D typescript ts-node-dev @types/node

3. npm install mongodb

4. npx tsc --init

5. npm install mongoose

Run:

1. npm tsx

2. node dict/server.js

For API Endpoint:

1. npm install express

2. npm install -D @types/express